import { writable } from 'svelte/store';

export const forward = writable(0);
export const backward = writable(0);
export const left = writable(0);
export const right = writable(0);
export const up = writable(0);
